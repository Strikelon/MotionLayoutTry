<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.strikalov.motionlayouttry">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".KeyAttributeActivity"></activity>
        <activity android:name=".KeyPositionActivity" />
        <activity android:name=".ImageFilterSample" />
        <activity android:name=".CustomBackgroundColor" />
        <activity android:name=".SquareAnimationListener" />
        <activity android:name=".SquareProgress2Activity" />
        <activity android:name=".SquareProgressActivity" />
        <activity android:name=".SquareEventActivity" />
        <activity android:name=".SquareClickActivity" />
        <activity android:name=".SquareScrollActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>